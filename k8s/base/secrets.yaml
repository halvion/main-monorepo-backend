# Kubernetes Secrets Template
# IMPORTANT: Replace values with base64 encoded secrets before applying
# Generate with: echo -n "your-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: halvion-secrets
  namespace: halvion
type: Opaque
data:
  # echo -n "your-jwt-secret-here" | base64
  JWT_SECRET: eW91ci1qd3Qtc2VjcmV0LWhlcmU=

  # Database passwords
  # echo -n "postgres-password" | base64
  POSTGRES_PASSWORD: cG9zdGdyZXMtcGFzc3dvcmQ=

  # RabbitMQ
  RABBITMQ_PASSWORD: Z3Vlc3Q=
